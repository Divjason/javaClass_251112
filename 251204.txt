static : 정적인 (사전적 의미)
> 클래스가 직접 관리하는 변수를 만들고 싶을 때

static 없으면 반드시 클래스 > 프로토타입 > 인스턴스를 통해서만 사용!!

static 존재한다면, 직접 클래스 호출을 통해서 사용!!!

static 메서드 > static 변수선언
자식요소에서 static 선언 x -> static 선언이 되어있는 부모요소가 해당 자식요소를 참조할 수 없음!